apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "RBCharts.fullname" . }}-test-connection"
  labels:
    {{- include "RBCharts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "RBCharts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
