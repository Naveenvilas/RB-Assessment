apiVersion: v1
kind: Service
metadata:
  name: {{ include "RBCharts.fullname" . }}
  labels:
    {{- include "RBCharts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "RBCharts.selectorLabels" . | nindent 4 }}
